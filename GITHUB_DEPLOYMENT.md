# GitHub 托管和部署指南

## 📦 项目说明

这是一个完整的个人记账管理系统，包含：
- ✅ 基础记账功能（收入/支出）
- ✅ 麻将记录管理
- ✅ 礼金记录管理（人情往来）
- ✅ 统计分析功能

## 🌐 托管到 GitHub

### 方案一：公开仓库（推荐学习/展示）

#### 优点
- ✅ 免费托管
- ✅ 可以作为作品集展示
- ✅ 他人可以学习和使用
- ✅ 支持 GitHub Pages（前端静态部署）

#### 注意事项
- ⚠️ **不要上传个人数据**（.gitignore 已配置）
- ⚠️ **不要上传数据库文件**（database/*.db）
- ⚠️ **不要上传环境变量**（.env 文件）

### 方案二：私有仓库（推荐个人使用）

#### 优点
- ✅ 完全私密
- ✅ 只有您可以访问
- ✅ 可以邀请特定协作者
- ✅ 适合个人数据管理

#### 注意事项
- 💰 GitHub 免费账户支持无限私有仓库
- ✅ 仍建议使用 .gitignore 保护敏感数据

## 🚀 上传到 GitHub 步骤

### 1. 初始化 Git 仓库

在项目根目录运行：

```bash
# 初始化 Git 仓库
git init

# 添加所有文件（.gitignore 会自动排除敏感文件）
git add .

# 查看将要提交的文件
git status

# 提交到本地仓库
git commit -m "初始提交：记账管理系统"
```

### 2. 创建 GitHub 仓库

1. 访问 https://github.com
2. 点击右上角 "+" → "New repository"
3. 填写仓库信息：
   - **Repository name**: `accounting-system`（或其他名称）
   - **Description**: 个人记账管理系统
   - **Public** 或 **Private**: 根据需求选择
   - **不要勾选** "Initialize with README"（我们已有README）
4. 点击 "Create repository"

### 3. 连接并推送到 GitHub

```bash
# 添加远程仓库（替换为您的GitHub用户名）
git remote add origin https://github.com/YOUR_USERNAME/accounting-system.git

# 推送到 GitHub
git branch -M main
git push -u origin main
```

### 4. 验证上传

访问您的 GitHub 仓库页面，应该能看到所有文件（除了 .gitignore 中排除的）

## 📥 在其他电脑上使用

### 克隆项目

```bash
# 克隆仓库
git clone https://github.com/YOUR_USERNAME/accounting-system.git

# 进入项目目录
cd accounting-system

# 安装后端依赖
npm install

# 初始化数据库
npm run init-db
npm run init-mahjong
npm run init-gifts

# 安装前端依赖
cd frontend
npm install
cd ..

# 启动后端
npm start

# 在新终端启动前端
cd frontend
npm start
```

## 🔒 数据安全建议

### 敏感文件保护

已在 `.gitignore` 中配置，以下文件**不会**上传到 GitHub：

- ✅ `database/*.db` - 数据库文件（包含个人数据）
- ✅ `.env` - 环境变量（包含密钥）
- ✅ `node_modules/` - 依赖包（自动安装）
- ✅ `test-*.js`, `debug-*.js` - 测试脚本

### 创建示例环境变量文件

```bash
# 创建 .env.example 文件（可以上传）
cat > .env.example << 'EOF'
# 数据库路径
DB_PATH=./database/accounting.db

# 服务器端口
PORT=3000

# JWT 密钥（请修改为复杂字符串）
JWT_SECRET=your-secret-key-change-in-production

# 环境模式
NODE_ENV=development
EOF
```

### 使用时复制配置

```bash
# 在新环境中
cp .env.example .env
# 然后编辑 .env 修改 JWT_SECRET
```

## 🌍 部署选项

### 选项1：本地使用（当前方式）
- ✅ 完全私密
- ✅ 数据在本地
- ✅ 无需服务器
- ❌ 只能在一台电脑上使用

### 选项2：内网部署
- ✅ 局域网内访问
- ✅ 数据在本地网络
- ✅ 多设备可访问
- ❌ 需要配置内网穿透

### 选项3：云服务器部署
**推荐平台：**
- **Railway**: https://railway.app （免费额度）
- **Render**: https://render.com （免费层）
- **Vercel**: https://vercel.com （前端免费，后端需付费）
- **Heroku**: https://heroku.com （需付费）

**优点：**
- ✅ 随时随地访问
- ✅ 自动部署
- ✅ HTTPS 加密

**缺点：**
- ⚠️ 数据在云端（注意隐私）
- 💰 可能需要付费

### 选项4：GitHub Pages（仅前端静态展示）
- ⚠️ **不推荐本项目** - 需要后端API
- 只适合纯静态网站

## 🛠️ Railway 部署示例（推荐）

Railway 提供免费的全栈部署：

### 1. 准备项目

添加 `Procfile` 或在 `package.json` 中确保有：
```json
{
  "scripts": {
    "start": "node server.js"
  }
}
```

### 2. 连接 GitHub

1. 访问 https://railway.app
2. 使用 GitHub 登录
3. 点击 "New Project" → "Deploy from GitHub repo"
4. 选择您的仓库

### 3. 配置环境变量

在 Railway 中设置：
- `DB_PATH`: `/app/database/accounting.db`
- `JWT_SECRET`: 生成一个复杂的密钥
- `PORT`: 默认即可

### 4. 初始化数据库

在 Railway 终端运行：
```bash
npm run init-db
npm run init-mahjong
npm run init-gifts
```

### 5. 部署前端

前端需要修改 API 地址：
```javascript
// frontend/src/utils/api.js
const api = axios.create({
  baseURL: process.env.REACT_APP_API_URL || 'http://localhost:3000/api',
  // ...
});
```

在 Railway 设置前端环境变量：
- `REACT_APP_API_URL`: `https://your-backend.railway.app/api`

## 📝 GitHub 仓库最佳实践

### README.md 完善

建议在 README 中添加：
- ✅ 项目截图
- ✅ 功能演示 GIF
- ✅ 详细的安装步骤
- ✅ 使用说明
- ✅ 技术栈说明

### 添加 LICENSE

选择合适的开源许可证：
- **MIT**: 最宽松，允许商业使用
- **GPL**: 要求衍生项目开源
- **私有仓库**: 可不添加

### 添加 CONTRIBUTING.md

如果是公开仓库，说明如何贡献：
```markdown
# 贡献指南

感谢您的贡献！

## 如何贡献
1. Fork 本项目
2. 创建特性分支 (git checkout -b feature/AmazingFeature)
3. 提交更改 (git commit -m 'Add some AmazingFeature')
4. 推送到分支 (git push origin feature/AmazingFeature)
5. 开启 Pull Request
```

## 🔐 安全建议

### 1. 保护敏感信息

**永远不要上传：**
- ❌ 数据库文件（.db）
- ❌ JWT 密钥（.env）
- ❌ 个人数据
- ❌ 密码或 token

### 2. 使用环境变量

所有敏感配置都通过环境变量：
```javascript
const JWT_SECRET = process.env.JWT_SECRET || 'default-key-only-for-dev';
```

### 3. 数据库备份

定期备份数据库文件到安全位置（不是 GitHub）：
```bash
# 本地备份
cp database/accounting.db backups/accounting_$(date +%Y%m%d).db
```

### 4. 双因素认证

为 GitHub 账户启用 2FA（两步验证）

## 📊 项目结构建议

### 保持清晰的目录结构

```
accounting-system/
├── backend/              # 可选：将后端代码移到这里
│   ├── config/
│   ├── controllers/
│   ├── models/
│   ├── routes/
│   ├── middleware/
│   └── server.js
├── frontend/             # 前端代码
│   ├── src/
│   └── package.json
├── database/
│   └── schema.sql       # 只上传表结构，不上传.db文件
├── scripts/
├── docs/                # 可选：文档目录
├── .gitignore
├── README.md
└── package.json
```

## 🎯 快速开始 - Git 命令

```bash
# 1. 初始化（如果还没有）
git init

# 2. 检查将要上传的文件
git status

# 3. 添加文件
git add .

# 4. 提交
git commit -m "feat: 完整的记账管理系统（麻将+礼金+记账）"

# 5. 创建 GitHub 仓库后，添加远程地址
git remote add origin https://github.com/YOUR_USERNAME/REPO_NAME.git

# 6. 推送
git branch -M main
git push -u origin main

# 7. 后续更新
git add .
git commit -m "fix: 修复麻将记录排序问题"
git push
```

## 📱 GitHub Actions（可选 - 自动化）

创建 `.github/workflows/test.yml` 实现自动测试：

```yaml
name: Test

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v2
    
    - name: Setup Node.js
      uses: actions/setup-node@v2
      with:
        node-version: '18'
    
    - name: Install dependencies
      run: npm install
    
    - name: Run tests
      run: npm test  # 如果有测试
```

## 🎉 总结

### 可以托管到 GitHub ✅

**推荐方式：**
1. **公开仓库** - 如果想分享代码或展示作品
2. **私有仓库** - 如果只是个人使用和备份

### 数据安全 ✅

- ✅ `.gitignore` 已配置，保护敏感文件
- ✅ 数据库不会上传
- ✅ 环境变量不会泄露

### 多端使用 ✅

**可以实现：**
1. 将代码托管到 GitHub
2. 在不同电脑克隆项目
3. 各自维护独立的数据库
4. 或部署到云服务器统一访问

### 需要注意 ⚠️

1. **数据库是本地的** - SQLite 文件不会自动同步
2. **如果需要多设备同步** - 考虑部署到云服务器
3. **敏感信息** - 确保 .gitignore 正确配置

---

**您的项目完全可以托管到GitHub！** 🎊

要开始吗？我可以帮您：
1. 初始化 Git 仓库
2. 检查要上传的文件
3. 创建初始提交

需要我帮您执行这些命令吗？

